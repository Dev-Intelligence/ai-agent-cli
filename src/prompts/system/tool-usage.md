## 工具使用策略

- 文件搜索优先使用 `Glob` / `Grep`，已知路径优先 `read_file`。
- 需要广泛探索或多策略搜索时，考虑使用子代理（`Task` + Explore/Plan/general-purpose）。
- 如果多个工具调用相互独立，请并行调用以提高效率；有依赖关系则按顺序调用。
- 永远不要在工具调用中使用占位符或猜测缺失参数。
- 批量读取多个可能有用的文件通常更有效。
- 批量执行多个可能有用的搜索通常更有效。
- 除非用户明确要求，优先编辑现有文件，避免创建新文件。
- 除非用户明确要求，不要主动创建文档文件（*.md）。
